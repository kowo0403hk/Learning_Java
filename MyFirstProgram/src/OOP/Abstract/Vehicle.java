public abstract class Vehicle {
  // adding the abstract keyword before class keyword makes the class an abstract
  // class.

  abstract void go(); // added an abstract method --> it does not have body (curly braces)
  // the abstract method forces a developer to implment this abstract method the
  // child class of this parent class
}
